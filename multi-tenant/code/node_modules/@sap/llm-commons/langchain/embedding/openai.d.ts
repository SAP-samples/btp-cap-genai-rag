import { OpenAIEmbeddingsParams, OpenAIEmbeddings } from 'langchain/embeddings/openai';
import { BaseLLMParams } from 'langchain/llms/base';
import { B as BTPBaseLLMParameters } from '../../base-cac7307b.js';
import { BTPOpenAIGPTEmbeddingModel } from '../../client/openai.js';
import 'axios';
import '../../config-822cfdeb.js';
import '@sap-cloud-sdk/util';
import 'zod-to-json-schema/src/parseDef.js';

/**
 * Input for Text generation for OpenAI GPT
 */
interface BTPOpenAIGPTEmbeddingInput extends Omit<OpenAIEmbeddingsParams, "modelName">, BTPBaseLLMParameters<BTPOpenAIGPTEmbeddingModel>, BaseLLMParams {
}
/**
 * OpenAI GPT Language Model Wrapper to embed texts
 */
declare class BTPOpenAIGPTEmbedding extends OpenAIEmbeddings implements BTPOpenAIGPTEmbeddingInput {
    private btpOpenAIClient;
    deployment_id: BTPOpenAIGPTEmbeddingModel;
    constructor(fields?: Partial<BTPOpenAIGPTEmbeddingInput>);
    embedDocuments(documents: string[]): Promise<number[][]>;
    embedQuery(query: string): Promise<number[]>;
    private createEmbedding;
}
/**
 * @deprecated Use {@link BTPOpenAIGPTEmbedding} instead
 */
declare const BTPOpenAIEmbedding: typeof BTPOpenAIGPTEmbedding;

export { BTPOpenAIEmbedding, BTPOpenAIGPTEmbedding, type BTPOpenAIGPTEmbeddingInput };
