import { GooglePaLMEmbeddingsParams, GooglePaLMEmbeddings } from 'langchain/embeddings/googlepalm';
import { BaseLLMParams } from 'langchain/llms/base';
import { B as BTPBaseLLMParameters } from '../../base-cac7307b.js';
import { BTPGooglePaLMEmbeddingModel } from '../../client/google.js';
import 'axios';
import '../../config-822cfdeb.js';
import '@sap-cloud-sdk/util';

/**
 * Input for Text generation for Google PaLM
 */
interface BTPGooglePaLMEmbeddingInput extends Omit<GooglePaLMEmbeddingsParams, "modelName" | "apiKey">, BTPBaseLLMParameters<BTPGooglePaLMEmbeddingModel>, BaseLLMParams {
}
/**
 * GooglePaLM Language Model Wrapper to embed texts
 */
declare class BTPGooglePaLMEmbedding extends GooglePaLMEmbeddings implements BTPGooglePaLMEmbeddingInput {
    private btpGooglePaLMClient;
    deployment_id: BTPGooglePaLMEmbeddingModel;
    constructor(fields?: Partial<BTPGooglePaLMEmbeddingInput>);
    embedQuery(document: string): Promise<number[]>;
    embedDocuments(documents: string[]): Promise<number[][]>;
    protected embedText(texts: string[]): Promise<number[][]>;
}

export { BTPGooglePaLMEmbedding, type BTPGooglePaLMEmbeddingInput };
