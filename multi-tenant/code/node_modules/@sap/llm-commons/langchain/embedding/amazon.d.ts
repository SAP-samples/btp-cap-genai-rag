import { EmbeddingsParams, Embeddings } from 'langchain/embeddings/base';
import { BaseLLMParams } from 'langchain/llms/base';
import { B as BTPBaseLLMParameters } from '../../base-cac7307b.js';
import { BTPAmazonTitanEmbeddingModel } from '../../client/amazon.js';
import 'axios';
import '../../config-822cfdeb.js';
import '@sap-cloud-sdk/util';

/**
 * Input for Text generation for Amazon Titan
 */
interface BTPAmazonTitanEmbeddingInput extends EmbeddingsParams, BTPBaseLLMParameters<BTPAmazonTitanEmbeddingModel>, BaseLLMParams {
}
/**
 * BTP Amazon Titan Language Model Wrapper to embed texts
 */
declare class BTPAmazonTitanEmbedding extends Embeddings implements BTPAmazonTitanEmbeddingInput {
    private btpAmazonTitanClient;
    deployment_id: BTPAmazonTitanEmbeddingModel;
    constructor(fields?: Partial<BTPAmazonTitanEmbeddingInput>);
    embedQuery(document: string): Promise<number[]>;
    embedDocuments(documents: string[]): Promise<number[][]>;
}

export { BTPAmazonTitanEmbedding, type BTPAmazonTitanEmbeddingInput };
