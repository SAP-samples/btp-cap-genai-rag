import { CallbackManagerForLLMRun } from 'langchain/callbacks';
import { AI21Input, AI21 } from 'langchain/llms/ai21';
import { BaseLLMParams } from 'langchain/llms/base';
import { BTPAI21JurassicTextModel } from '../../client/ai21.js';
import { B as BTPBaseLLMParameters } from '../../base-cac7307b.js';
import 'axios';
import '../../config-822cfdeb.js';
import '@sap-cloud-sdk/util';

/**
 * Input for Text generation for AI21 Jurassic
 */
interface BTPAI21JurassicInput extends Omit<AI21Input, "modelName" | "ai21ApiKey" | "logitBias" | "baseUrl">, BTPBaseLLMParameters<BTPAI21JurassicTextModel>, BaseLLMParams {
    /**
     * The number of top-scoring tokens to consider for each generation step.
     *
     * Return the top-K alternative tokens. When using a non-zero value, the response includes the string representations
     * and logprobs for each of the top-K alternatives at each position, in the prompt and in the completions.
     *
     * @default 0
     */
    topKReturn?: number;
}
/**
 * AI21 Language Model Wrapper to generate texts
 */
declare class BTPAI21Jurassic extends AI21 implements BTPAI21JurassicInput {
    private btpAI21JurassicClient;
    deployment_id: BTPAI21JurassicTextModel;
    topKReturn?: number;
    constructor(fields?: Partial<BTPAI21JurassicInput>);
    _call(prompt: string, options: this["ParsedCallOptions"], runManager?: CallbackManagerForLLMRun): Promise<string>;
}

export { BTPAI21Jurassic, type BTPAI21JurassicInput };
