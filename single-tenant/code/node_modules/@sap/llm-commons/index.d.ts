import { B as BTPLLMConfigParameters } from './config-822cfdeb.js';

/**
 * BTP LLM Context
 *
 * Note: This is a singleton instance to be shared across all BTP LLM client usages.
 */
declare class BTPLLMContext {
    /**
     * BTP LLM Context Singleton
     */
    private static instance;
    /**
     * BTP LLM Configuration
     */
    private config;
    private constructor();
    /**
     * Initializes the context such as authentication, configuration etc.
     * @param params (optional) configuration parameters
     * @returns
     */
    static init(params?: Partial<BTPLLMConfigParameters>): Promise<BTPLLMContext>;
    /**
     * Gets the configuration from context
     * @returns the configuration
     * @throws {@link BTPLLMError} when the context is not initialized
     */
    static getConfig(): BTPLLMConfigParameters;
}

export { BTPLLMContext };
